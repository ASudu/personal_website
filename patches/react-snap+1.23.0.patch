diff --git a/node_modules/react-snap/src/puppeteer_utils.js b/node_modules/react-snap/src/puppeteer_utils.js
index c4b7641..3bb828d 100644
--- a/node_modules/react-snap/src/puppeteer_utils.js
+++ b/node_modules/react-snap/src/puppeteer_utils.js
@@ -211,7 +211,16 @@ const crawl = async opt => {
     if (!shuttingDown && !skipExistingFile) {
       try {
         const page = await browser.newPage();
-        await page._client.send("ServiceWorker.disable");
+        const cdpClient =
+          typeof page._client === "function"
+            ? page._client()
+            : page._client;
+        if (cdpClient && typeof cdpClient.send === "function") {
+          await cdpClient.send("ServiceWorker.disable");
+        } else {
+          const fallbackClient = await page.target().createCDPSession();
+          await fallbackClient.send("ServiceWorker.disable");
+        }
         await page.setCacheEnabled(options.puppeteer.cache);
         if (options.viewport) await page.setViewport(options.viewport);
         if (options.skipThirdPartyRequests)
diff --git a/node_modules/react-snap/src/tracker.js b/node_modules/react-snap/src/tracker.js
index 6f813f4..255885c 100644
--- a/node_modules/react-snap/src/tracker.js
+++ b/node_modules/react-snap/src/tracker.js
@@ -15,9 +15,13 @@ const createTracker = page => {
   return {
     urls: () => Array.from(requests).map(r => r.url()),
     dispose: () => {
-      page.removeListener("request", onStarted);
-      page.removeListener("requestfinished", onFinished);
-      page.removeListener("requestfailed", onFinished);
+      const detach =
+        typeof page.removeListener === "function"
+          ? page.removeListener.bind(page)
+          : page.off.bind(page);
+      detach("request", onStarted);
+      detach("requestfinished", onFinished);
+      detach("requestfailed", onFinished);
     }
   };
 };
